// currents.config.js
module.exports = {
   projectId: "nijat", // the projectId, can be any values for sorry-cypress users
   recordKey: "somekey", // the record key, can be any value for sorry-cypress users
   cloudServiceUrl: "http://localhost:1234", // Sorry Cypress users - set the director service URL
   video: true,
}
